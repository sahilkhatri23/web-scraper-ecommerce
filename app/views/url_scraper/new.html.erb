<h1>Scrape Categories and Products</h1>
<%= form_with url: url_scraper_index_path, method: :post, local: true do |form| %>
  <div>
    <%= form.label :url, "Enter URL to Scrape:" %>
    <%= form.text_field :url, required: true %>
  </div>
  <div>
    <%= form.submit "Scrape" %>
  </div>
<% end %>